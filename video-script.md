# AI Helper 参赛演示视频脚本

## 一、视频概述

| 项目 | 建议 |
|------|------|
| 视频时长 | 3-5 分钟（建议控制在 4 分钟以内） |
| 分辨率 | 1920x1080（1080p） |
| 录屏工具 | OBS Studio（免费）/ QuickTime Player（Mac）/ ScreenFlow |
| 录音环境 | 安静室内，使用领夹麦或 USB 麦克风 |
| 语速 | 每分钟 220-260 字，比日常说话稍慢 |
| 字幕 | 建议加字幕（可用剪映自动生成 + 人工校对） |

## 二、录制准备清单

### 环境准备
- [ ] Chrome 浏览器已安装 AI Helper 扩展（dist 目录加载）
- [ ] 已配置 API Key（DeepSeek 或其他 OpenAI 兼容 API）
- [ ] 已连接本地 Agent 服务（`aha start -b`）
- [ ] 准备好演示用网页（如一个包含表格的页面、一个需要填写的表单）
- [ ] 关闭通知、微信等弹窗干扰
- [ ] 浏览器缩放设为 100% 或 110%（确保文字清晰）

### 录屏设置
- [ ] OBS 录制区域：完整浏览器窗口
- [ ] 帧率：30fps
- [ ] 音频：麦克风单独音轨
- [ ] 鼠标高亮：开启（让观众看清点击位置）

## 三、视频分镜脚本

---

### 【开场】0:00 - 0:30（约 30 秒）

**画面：** 黑屏渐入 → 软件封面图 → 切到 Chrome 浏览器全景

**旁白：**

> 每天我们在浏览器上花费大量时间——读文档、填表单、处理数据。
> 但现有的 AI 工具，要么是独立的聊天窗口，无法感知你正在看的页面；
> 要么只能被动回答问题，无法替你操作浏览器。
>
> 如果 AI 能像人类一样，看懂网页内容，还能自己点击、填表、提取数据呢？
>
> 这就是 AI Helper——基于浏览器端的 AI 办公助手插件。

**画面提示：** 说到"看懂网页内容"时展示 AI Helper 侧边栏打开的特写

---

### 【项目介绍】0:30 - 1:00（约 30 秒）

**画面：** AI Helper 侧边栏全貌 → 镜头缓慢移动展示各功能区

**旁白：**

> AI Helper 是一款 Chrome 浏览器扩展，采用 ReAct 推理循环架构。
> 它不只是聊天工具，而是一个能真正操作浏览器的智能 Agent。
>
> 内建 40 多项工具，支持自然语言对话、浏览器自动化、网页内容提取、
> 文件问答、多智能体协作，并通过 MCP 协议实现无限扩展。
>
> 接下来，我用几个实际场景展示它的能力。

**画面提示：** 展示侧边栏各 Tab（工具选择面板、Agent 选择器、工作目录等）

---

### 【场景一：智能网页操作】1:00 - 1:50（约 50 秒）

**画面：** 打开一个网页（如搜索引擎或表单页面）

**旁白：**

> 第一个场景：智能网页操作。
>
> 我只需要用自然语言告诉 AI 我想做什么。
>
>（操作：在输入框输入指令）
> 比如我说："帮我在搜索框输入'开源软件'，然后点击搜索按钮。"
>
>（操作：发送消息，观察 AI 执行过程）
>
> 看，AI 首先会分析页面，找到可交互的元素，然后自动完成点击和输入。
> 整个过程通过 ReAct 推理循环完成——AI 思考、决策、调用工具、获取反馈、继续推理。
>
> 它甚至能穿透 Shadow DOM，操作 React、Vue 等现代框架渲染的组件。

**画面提示：**
- 展示输入指令的过程
- 展示执行日志面板（工具调用步骤）
- 展示 AI 自动操作网页的过程

**操作要点：**
- 提前准备好目标网页
- 指令要简单明了
- 确保执行日志面板打开，展示 AI 的推理过程

---

### 【场景二：数据提取与导出】1:50 - 2:30（约 40 秒）

**画面：** 打开一个包含表格数据的网页

**旁白：**

> 第二个场景：数据提取。
>
> 网页上的表格数据，以前需要手动复制粘贴。
> 现在，我只需要说："提取这个页面的表格数据，导出为 Excel。"
>
>（操作：发送指令，观察 AI 提取过程）
>
> AI 调用页面内容提取工具，将表格数据结构化为 Markdown 表格，
> 然后一键导出为 Excel 文件。整个过程不到 10 秒。
>
> 对于长页面，还支持滚动收集功能，自动滚动并去重聚合内容。

**画面提示：**
- 展示 AI 提取的表格渲染结果
- 展示导出 Excel 的操作
- 展示工具调用的执行日志

---

### 【场景三：划词问答】2:30 - 3:00（约 30 秒）

**画面：** 在任意网页选中文本

**旁白：**

> 第三个场景：划词即用。
>
> 在任意网页选中文本，自动弹出浮动工具栏。
> 可以快速进行 AI 搜索、解释、翻译、总结——无需切换到其他应用。
>
>（操作：选中一段文本，点击"解释"按钮）
>
> 结果在浮动面板中展示，支持拖拽缩放，还能继续追问。
> 工具栏按钮可以自定义，支持按域名屏蔽。

**画面提示：**
- 选中网页文本 → 浮动工具栏弹出
- 点击"解释"→ 结果面板展开
- 展示追问功能

---

### 【场景四：多 Agent 协作与反思系统】3:00 - 3:40（约 40 秒）

**画面：** 切换到 Agent 管理界面 → 执行一个复杂任务

**旁白：**

> 第四个场景：多 Agent 协作。
>
> AI Helper 内置 5 种专业助手模板，还可以创建自定义 Agent，
> 每个助手拥有独立的系统提示词和工具权限。
>
>（操作：展示 Agent 切换 → 执行任务）
>
> 更重要的是，AI Helper 创新性地引入了三级反思系统：
> 工具级反思——评估每次工具调用的结果质量；
> 子任务反思——评估子任务的完整性；
> 后置反思——对最终答案进行 7 个维度的质量评分。
>
>（操作：展示反思评分面板）
>
> 只有质量评分达到 7 分以上才会输出，否则自动修订或重试。
> 这确保了 AI 的输出是可靠的，而不是简单的原始结果。

**画面提示：**
- 展示 Agent 选择器切换
- 展示多智能体管理界面
- 展示后置反思的 7 维度雷达图和评分

---

### 【场景五：文件管理与 MCP 扩展】3:40 - 4:20（约 40 秒）

**画面：** 展示工作目录面板 → 展示 MCP 工具箱

**旁白：**

> 连接本地 Agent 服务后，AI Helper 可以直接在侧边栏管理本地文件：
> 目录浏览、文件预览、上传下载、甚至执行终端命令。
>
>（操作：展示工作目录面板 → 打开一个文件预览）
>
> 命令执行采用三级安全机制——黑名单禁止、灰名单确认、白名单放行，
> 配合路径沙箱和审计日志，确保安全可控。
>
>（操作：切到选项页 → 展示 MCP 工具箱）
>
> 通过 MCP 协议，可以连接任意第三方工具服务器，工具自动注册到系统中。
> 从数据库查询到 API 调用，能力无限扩展。

**画面提示：**
- 展示工作目录树和文件预览
- 展示 MCP 和 Skill 工具箱配置页面

---

### 【创新总结】4:20 - 4:50（约 30 秒）

**画面：** 文字动画依次弹出创新点 → 配合功能截图快闪

**旁白：**

> 回顾 AI Helper 的核心创新：
>
> 一、真正的浏览器操控能力——AI 像人类一样操作网页
> 二、三级反思质量保障——7 维度评分确保输出可靠
> 三、工具预筛选与 Token 优化——大幅降低 API 成本
> 四、多 Agent 协作与任务拆解——复杂任务并行处理
> 五、MCP 协议无限扩展——工具能力即插即用
> 六、断点续接与任务恢复——工程级可靠性保障
>
> 同时支持 Chrome 扩展和 Electron 桌面应用两种形态。

**画面提示：** 创新点文字逐条弹出，配合对应功能截图快速切换

---

### 【结尾】4:50 - 5:00（约 10 秒）

**画面：** GitHub 仓库地址 → 大赛 Logo → 渐黑

**旁白：**

> AI Helper——让浏览器会思考的智能助手。
>
> 项目已开源，采用 MIT 协议，欢迎访问 GitHub 仓库了解更多。
>
> 我是席维成，感谢观看。

**画面提示：**
- 显示 GitHub 仓库链接
- 显示开源协议 MIT License
- 渐黑结束

---

## 四、录制技巧建议

### 旁白录制
1. **分段录制**：按场景分段录制旁白，不要一次录完，方便重录不好的片段
2. **试读一遍**：正式录制前先读一遍，检查语句是否顺口
3. **语气**：自然、自信，像在跟朋友介绍一个好用的工具，不要像背课文
4. **停顿**：每个场景之间停顿 1-2 秒，方便后期剪辑

### 屏幕录制
1. **先演练**：每个场景的操作步骤先演练 2-3 遍，确保流畅
2. **操作节奏**：AI 处理需要等待时间，录制时可以等结果出来再继续说
3. **鼠标动作**：点击前稍作停顿，让观众看清你要操作哪里
4. **画面干净**：关闭无关的浏览器标签页和扩展图标

### 后期剪辑
1. **加速处理**：AI 等待响应的部分可以适当加速（1.5x-2x），保留关键步骤
2. **添加标注**：重要操作位置添加箭头或方框高亮标注
3. **背景音乐**：选择轻快的纯音乐，音量控制在旁白的 10-15%
4. **转场**：场景之间用简单的淡入淡出即可，不要太花哨
5. **字幕**：建议全程加字幕，方便静音观看

### 工具推荐
| 用途 | 工具 | 说明 |
|------|------|------|
| 屏幕录制 | OBS Studio | 免费，支持多场景、画中画 |
| 音频录制 | Audacity | 免费音频编辑，降噪处理 |
| 视频剪辑 | 剪映 / DaVinci Resolve | 剪映上手快，DaVinci 更专业 |
| 字幕生成 | 剪映自动字幕 | 识别准确率高，手动校对即可 |
| 标注动画 | 剪映贴纸 / After Effects | 箭头、方框等简单标注 |

## 五、时间分配建议

| 段落 | 时长 | 占比 |
|------|------|------|
| 开场引入 | 30s | 10% |
| 项目介绍 | 30s | 10% |
| 场景一：网页操作 | 50s | 17% |
| 场景二：数据提取 | 40s | 13% |
| 场景三：划词问答 | 30s | 10% |
| 场景四：Agent 协作 | 40s | 13% |
| 场景五：文件与 MCP | 40s | 13% |
| 创新总结 | 30s | 10% |
| 结尾 | 10s | 4% |
| **合计** | **约 4 分 50 秒** | **100%** |

## 六、注意事项

1. **真实性**：所有操作必须是真实运行的效果，不要用后期特效模拟功能
2. **完整性**：展示从输入到输出的完整流程，不要只展示结果
3. **稳定性**：提前测试每个场景，确保演示过程不报错
4. **开源信息**：结尾展示 GitHub 仓库链接和开源协议
5. **字幕校对**：AI 生成的字幕一定要人工校对，尤其是技术术语
